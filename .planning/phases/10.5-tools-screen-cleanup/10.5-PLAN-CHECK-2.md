# Phase 10.5 Plan Re-Check

## Verdict
PASS

## Fix verification

### Fix 1 — NULL guards on redeem_promotion
APPLIED — lines 352–358. NULL guard sits after `BEGIN` (line 351) and before the authz EXISTS check (lines 364–369). Raises `'invalid_input'` with `ERRCODE = '22023'` and `HINT = 'NULL_NOT_ALLOWED'` (lines 356–357). Prose at line 352–354 explicitly justifies the pre-authz placement as shape/type validation. Task 1.5 description (line 464) restates the ordering for future reviewers.

### Fix 2 — Bulk RPC WHERE clause
APPLIED — lines 315–319. All three filter conditions present:
- `b.start_time::DATE = p_date` (line 316) — uses `start_time::DATE`, not `b.booking_date`.
- `b.start_time > now()` (line 317) with inline comment "past-time guard parity with single RPC".
- `b.user_id IS NOT NULL` (line 319) with inline comment "guest rows skipped silently".
Task 1.4 description (line 457) repeats all three conditions and explicitly cites `start_time::DATE` mirroring `get_booking_heatmap` and the RESEARCH never having verified `booking_date`.

### Fix 3 — Authz-first ordering in redeem_promotion
APPLIED — strict ordering in the RPC body:
1. NULL guards (lines 355–358).
2. Authz EXISTS check raising `'not_found'` / `42501` (lines 364–372).
3. Range check on `p_discount_amount > 0` raising `'invalid_amount'` / `22023` + HINT `AMOUNT_MUST_BE_POSITIVE` (lines 375–378).
4. `FOR UPDATE` lock + usage_limit (lines 381–392).
5. Idempotent insert (lines 396–402).
6. Conditional counter bump (lines 404–416).
Prose pin at lines 360–363 ("authz precedes range validation so a non-owner cannot probe parameter ranges") and Task 1.5 description (line 464, "A future reviewer must not move the discount-amount check above authz") lock the ordering for future reviewers.

### Fix 4 — Definition of Done grep gate
APPLIED — line 673. The DoD now uses `grep -rE "state\.copyWith\([^)]*error: e\.toString\(\)" ...` directly targeting the leak pattern on the three named controllers (`reminders,promotions,export`). Inline justification calls out the `fields:` false-negative the old grep was vulnerable to. Regex matches `state.copyWith(<any args>error: e.toString()` — covers the exact leak shape.

## Collateral damage scan
None. File still ends with `## PLAN COMPLETE` at line 682. Migration ordering (enum → backfill → three RPCs) preserved at lines 26–31 and §Rollout lines 655–657. Risk register, checklist matrix, task estimates (670 min total), and all other invariants from the prior check unchanged.

## RECHECK COMPLETE
